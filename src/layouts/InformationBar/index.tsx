import React from 'react';

type Props = {
  title?: string;
  isBorderBottom?: boolean;
};

const InformationBar = (props: Props) => {
  return <div>InformationBar</div>;
};

export default InformationBar;
